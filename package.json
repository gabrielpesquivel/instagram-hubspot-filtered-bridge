{
  "name": "bootink-internal-tools",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build:dashboard": "cd dashboard && npm install && npm run build",
    "build:gangsheet": "bash scripts/build-gangsheet-bundle.sh",
    "deploy": "npm run build:gangsheet && npm run build:dashboard && wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250204.0",
    "typescript": "^5.7.3",
    "wrangler": "^4.65.0"
  }
}
